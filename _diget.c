#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <malloc.h>

char*  _diget(struct _num in)
{

	int *result;
	int length;

	if (length == 1)

	{
		if (in.flag == "d")

		{
			return (in.va);
			printf("this is d\n");
		}

		else if (in.flag == "i")

		{
			return (in.va);
			printf("this is i\n");
		}

		else
			return (NULL);

	{
}
