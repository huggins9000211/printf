#include "holberton.h"

char* _csget(str in)
{
	/*
	This can have length > 1






	*/



	if (in.flag == 'c')

	{
		return (in.va);
	}

	else if (in.flag == 's')

	{
		return (in.va);
	}

	else
		return (NULL);


}
